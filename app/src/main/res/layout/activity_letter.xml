<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/lettertv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="@drawable/bg_letter_text"
        android:gravity="center"
        android:text="A"
        android:textColor="#ff0000"
        android:textSize="15sp"
        android:visibility="gone" />

    <com.frank.customview.wight.lettersidebar.LetterSideBar
        android:id="@+id/lettersidebar"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:paddingRight="10dp"
        app:sideBarColor="@color/colorPrimary"
        app:sideBarHeightColor="@color/colorAccent"
        app:sideBarSize="15sp" />

</RelativeLayout>